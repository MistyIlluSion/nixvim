{
  config.opts = {
    # Line number
    number = true;
    relativenumber = true;

    # Highlight
    cursorline = true;

    # Tab
    tabstop = 2;
    shiftwidth = 2;
    expandtab = true;

    # Split window
    splitbelow = true;
    splitright = true;

    # Search
    ignorecase = true;
    smartcase = true;
    hlsearch = false;

    # Using lualine, no need to show mode
    showmode = false;

    # Clipboard
    clipboard = "unnamedplus";
  };
}
